export enum PublicRoutes {
  Home = '/',
  Connexion = '/connexion',
}

export enum AccountRoutes {
  Profile = '/profil',
  Users = '/utilisateurs',
}

export enum ServiceRoutes {
  Drugs = '/drugs',
}
